# Hint 3: the shape

The whole endpoint, with the blanks left for you to fill in.

```csharp
api.MapPost("/bids", async (PlaceBidBody body, AuctionService.AuctionServiceClient auction, CancellationToken ct) =>
{
    try
    {
        var response = await auction.PlaceBidAsync(
            new PlaceBidRequest { /* three properties, all from body */ },
            deadline: /* two seconds from now */,
            cancellationToken: ct);
        return Results.Ok(/* response.Auction, as JSON for the browser */);
    }
    catch (RpcException ex)
    {
        return /* the same as GET /auction */;
    }
});
```

Filled it in? Save, wait a few seconds, and bid. Still red? The full answer is under
[Out of time?](../README.md#out-of-time) in the guide.
